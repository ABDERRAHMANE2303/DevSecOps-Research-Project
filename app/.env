# Database Configuration
# IMPORTANT: Change these values to match your database setup
# DO NOT commit this file to version control with real credentials

DB_HOST=localhost
DB_NAME=countries
DB_USER=webapp_user
DB_PASSWORD=your_password

# Environment (development or production)
APP_ENV=development

# Display errors (set to false in production)
DISPLAY_ERRORS=true
